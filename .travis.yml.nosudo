language: rust
rust:
  - stable
  - beta
  - nightly
sudo: false
matrix:
  allow_failures:
    - rust: nightly

addons:
  apt:
    packages:
      - libsdl2-dev
      - libsdl2-image-dev
      - libsdl2-ttf-dev